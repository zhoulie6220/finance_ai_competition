# 财报叙事一致性分析与情景估值投研工作台 —— 后端依赖
#
# 说明：本文件声明直接依赖与最低版本。真正的可复现锁定版本请用
#   python -m pip freeze > requirements.lock.txt
# 生成后随代码提交（赛事硬性要求「提供依赖文件及运行说明」）。
#
# 安装：
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   python -m pip install -r requirements.txt

# ---- Web 框架 ----
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12        # 文件上传（multipart/form-data）

# ---- 数据与校验 ----
pydantic>=2.9
pydantic-settings>=2.6
pandas>=2.2

# ---- 文档解析 ----
pymupdf>=1.24                   # PDF 文本与坐标。许可证 AGPL-3.0，见 docs/07-third-party-licenses.md
pdfplumber>=0.11                # 表格抽取。MIT
# OCR 兜底（扫描页）。按需安装，二者择一：
#   pytesseract>=0.3            # 需另装 Tesseract 二进制
#   paddleocr>=2.8              # 体积较大

# ---- 模型接口（OpenAI 兼容，指向 DeepSeek）----
openai>=1.50

# ---- 测试 ----
pytest>=8.0
httpx>=0.27                     # FastAPI TestClient 依赖

# 说明：数据库用 Python 标准库 sqlite3，无需安装任何 SQL 软件；
#      检索用 SQLite FTS5（内置），不引入向量库。
