# ---- Node / 前端 ----
node_modules
dist
dist-ssr
*.local
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ---- Python / 后端 ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 运行期产物：SQLite 库、上传文件、页面截图、导出件
backend/var/
*.db
*.db-wal
*.db-shm

# 密钥与本地配置（.env.example 需入库，.env 不入库）
.env
.env.local
!.env.example

# 样例年报 PDF 体积较大（每家约 5–20MB，9 份可达上百 MB），不入库。
# 提取后的结构化产物与黄金标注集需入库，见 backend/app/data/。
backend/app/data/samples/raw/
# 原始年报 PDF：体积大（约 70MB），且为公开披露材料，不入库。
# 解析产物随代码提交（scripts/parse_reports.py 可重新生成）。
3份公司年报及年报摘要/

# ---- 编辑器 ----
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
