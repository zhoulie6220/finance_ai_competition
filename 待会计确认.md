# 待会计确认的 7 件事

> ### 计算机同学和你的 AI 读这份。会计同学读的是你贴到群里的那段话。

---

## 怎么用（AI 看这里）

这 7 件事是**还没经会计确认的口径**——**已经实现、正在生效，但没人签过字**。

**要在相关模块上动手之前，先去群里问。** 每一题都有一段
「**群里怎么问**」，用 `>` 引用，**可以直接整段复制粘贴**——
那段话是写给不看代码的人的，自带背景，会计同学不用打开仓库也能答。

拿到答复之后：

1. **把答复填回本文对应的「答」下面**，写上日期和是谁说的。
   ⚠ **不要删题。** 这里是决策记录：只留在聊天记录里的话，
   下一次开新会话的 AI 会重新问一遍，或者更糟——默认现值就是对的。
2. 改代码/种子（每题的「落地位置」在下半部分）
3. 去掉 `rule_config` 里对应的 `⬜ 待会计确认` 前缀
4. 按 `CLAUDE.md` 的三步重建：`init_db.py --force` → `parse_reports.py` → `parse_mdna.py`

**为什么必须问，不能自己拍**

系统会算出「叙事一致性」分数，再拿它调估值。但有 12 个参数是写代码的同学为了先跑起来
自己填的，**没有任何会计依据**。它们在系统里和「已经确认过」的参数**完全一样**，
机器分不出来。其中几个直接决定估值区间——会出现在给评审看的结论里。

---

## 问题 1｜同一科目正负号跨年不一致 🔴

**群里怎么问**（可直接复制）

> 有个口径要定一下。年报里「资产减值损失」这个科目，2015 年报印的是正数
> （表示损失了多少），2024 年报印的是负数（表示抵减了多少利润），方向是反的。
> 这是列报习惯的变化，不是数据错误。
>
> 但系统要把十年的数放在一起比大小，而 +3 亿 和 −3 亿 在比大小时方向完全相反。
>
> 想问：这类科目该按哪种口径统一？哪些科目算「正数表示好」、哪些算「负数表示好」？
> （不用看代码，给个口径就行）

**为什么问你**：这是年报列报习惯的变化，资产减值损失在钢铁行业是常态科目，
十年里必然跨过口径切换。不定的话，某个主题的「冲突」会凭空多出来，而且不报错。

**落地位置**：`0001_metric_definitions.sql` 的 `sign_convention`；解析层按它归一

**答**：

---

## 问题 2｜管理层说了某类话，该拿哪张表去验？ 🔴

**群里怎么问**（可直接复制）

> 系统会从年报的「管理层讨论与分析」里认出管理层的说法，再拿一个财务指标去对。
> 「认哪些说法」有依据（你们文档里列了五类），但**每类该拿哪个指标去验，
> 是写代码的同学自己配的**。现在是这样：
>
> | 管理层说的 | 现在拿这个指标验 |
> |---|---|
> | 需求旺盛、产销两旺 | 营业收入 |
> | 满负荷生产、产能利用率提升 | 营业收入 |
> | 降本增效、成本削减 | 毛利率 |
> | 产品结构优化、高端产品 | 毛利率 |
> | 回款改善、现金流改善 | 经营活动现金流量净额 |
>
> 想问：这五组配对对不对？
>
> 特别想问「降本增效」为什么验毛利率而不是成本额本身——现在的理由是
> 「成本额会随产量一起涨，涨得比收入慢才叫降本」。这条评委很可能会问。

**为什么问你**：**配错了，「叙事与事实相悖」就是误报**——
而这正是整个系统要给评审看的核心结论。

**落地位置**：`engine/narrative.py::_THEMES` 的 `metric` 与 `basis_cn`

**答**：

---

## 问题 3｜「产能释放」算利好还是利空？ 🟡

**群里怎么问**（可直接复制）

> 系统的词表里**没有收**「产能释放」。原因是宝钢 2024 年报的原话是：
>
> > 市场有效需求不足、钢铁产能释放较快，铁矿石价格高位运行，行业盈利空间受到挤压
>
> 在这个语境里产能释放说的是供给过剩，**是利空**。如果按字面当成
> 「公司看好产能扩张」，系统会在一份基调悲观的年报上判出乐观结论。
>
> 想问：不收，对吗？（不收的代价是这一类说法一条都识别不出来。）

**落地位置**：`_THEMES` 里 `capacity` 主题的 `keywords` / `exclude`

**答**：

---

## 问题 4｜分数低的时候，估值该往悲观调多少？ 🔴

**群里怎么问**（可直接复制）

> 系统算出「叙事一致性」分数后，会按下表调整估值（三档情景的权重，
> 以及增长率、折现率的变化）：
>
> | 分数 | 情景权重（基准/乐观/压力） | 参数怎么调 |
> |---|---|---|
> | 高（≥70） | 60% / 25% / 15% | 增长率取历史中位数，折现率与永续增长率不动 |
> | 中（45–69） | 50% / 20% / 30% | 增长率取历史下四分位，折现率 +0.5 个百分点 |
> | 低（<45） | 35% / 15% / 50% | 增长率取下四分位再打九折，折现率 +1.0 个百分点，永续增长率 −0.5 个百分点 |
> | 证据不足 | 不调 | 什么参数都不动，只提示人工复核 |
>
> 你们给的文档里这部分**只写了方向**（原话是「保持基准情景；**可提高**基准情景
> 权重」），**一个具体数字都没有**，所以这些是写代码的同学先填的。
>
> 想问：这个量级合适吗？最想确认的是——**分数一低就把压力情景的权重推到 50%**，
> 会不会太激进？

**为什么问你**：整张表直接决定估值区间。周期股估值的核心争议就在情景权重。

**落地位置**：`0002_rule_config.sql` 的 `mapping.*`；
`docs/04-index-rules.md` §五；`engine/index.py::map_index_to_scenario()`（未实现）

**答**：

---

## 问题 5｜一次「叙事与事实相悖」，该扣多少分？ 🟡

**群里怎么问**（可直接复制）

> 分数由三类观测构成，现在按这个权重加总：
>
> | 观测 | 权重 |
> |---|---|
> | 被财务事实验证 | 1.0 |
> | 方向一致但幅度偏弱（部分支持） | 0.5 |
> | 与财务事实相悖 | −1.0 |
>
> 另外「幅度偏弱」的分界线现在定在 0.5（实际幅度不到目标幅度的一半就算部分支持）。
>
> 你们的文档只说了「方向一致但幅度较弱，标记为部分支持」，没给具体比例。
> 想问：这三个权重合不合适？
> （「相悖」的负权重定多重，等于定了「一次冲突扣多少分」，所以想请你定。）

**落地位置**：`0002_rule_config.sql` 的 `index.weight.*` 与 `index.direction.weak_ratio`

**答**：

---

## 问题 6｜估值的两个上下限 🟡

**群里怎么问**（可直接复制）

> 估值里有两个上下限：
>
> - **永续增长率上限 2%** —— 周期行业不给高永续增速，按长期通胀封顶
> - **折现率下限 6%** —— 防止压力情景下折现率被推到不切实际的水平
>
> 方向是你们定的（周期股估值硬规则），但 2% 和 6% 这两个数字是写代码的同学填的。
> 想问：这两个数合适吗？

**落地位置**：`0002_rule_config.sql` 的 `valuation.terminal_growth_max` / `wacc_min`

**答**：

---

## 问题 7｜一个后果，需要你确认能不能接受

**群里怎么问**（可直接复制）

> 写代码的同学原先在系统里放了一道「小幅波动不算数」的规则——**变动不到 1%
> 就当作没动**。理由是这样两条会被判成冲突：
>
> ```
> [降本增效] 2020 年｜毛利率 10.8791% → 10.8350%（-0.04 个百分点）
> ```
>
> 但这条规则和你们给的文档**直接冲突**。文档原文是：
>
> > 「20 个百分点」只适用于明确提出数值目标的情况。对于「需求增长」「回款改善」
> > 等方向性表述，**实际方向相反，直接标记为冲突**。
>
> 所以那道 1% 的规则已经删掉了。删掉之后，上面那条真的被判成了冲突，
> 全样本的冲突数从 8 条变成 10 条。
>
> 想问：0.04 个百分点这个量级判成「叙事与事实相悖」，能接受吗？
> 不能接受的话，**请在文档里写一条明确的下限**（例如「方向性主张的幅度下限为
> X 个百分点」）。

**落地位置**：已执行（删除了 `narrative.min_rel_change`）；只需确认，或补一条明确下限

**答**：

---

## 附：不用管的（免得白花时间）

| 事情 | 情况 |
|---|---|
| `docs/04-index-rules.md` 一直没交 | 是个误会。指数公式你们 9-22 就给了，只是没人把它转成文档，现在已经转好 |
| 参数数量对不上（65 还是 66） | 对得上，中间改过一次，现在是 65，和文档里说的一致 |
| 系统里有个「1% 阈值」 | 已按你们的文档删除，见问题 7 |

---
---

# （以下给计算机同学，会计同学可以跳过）

## 被标记的参数

`rule_config` 里 `description` 带 `⬜ 待会计确认` 前缀的，就是上面这些问题涉及的参数：

| 参数 | 现值 | 对应问题 |
|---|---|---|
| `index.direction.weak_ratio` | `0.5` | 问题 5 |
| `index.weight.supported` | `1.0` | 问题 5 |
| `index.weight.partial` | `0.5` | 问题 5 |
| `index.weight.conflicted` | `-1.0` | 问题 5 |
| `mapping.high.weights` | `[0.60,0.25,0.15]` | 问题 4 |
| `mapping.high.delta` | `historical_median` | 问题 4 |
| `mapping.medium.weights` | `[0.50,0.20,0.30]` | 问题 4 |
| `mapping.medium.delta` | `historical_p25` | 问题 4 |
| `mapping.low.weights` | `[0.35,0.15,0.50]` | 问题 4 |
| `mapping.low.delta` | `historical_p25_x0.9` | 问题 4 |
| `valuation.terminal_growth_max` | `0.02` | 问题 6 |
| `valuation.wacc_min` | `0.06` | 问题 6 |

> 这张表和种子文件由 `backend/tests/unit/db/test_pending_accounting.py` 双向对账：
> 种子里有标记而表里没有、或表里有而种子没标记，**都会测试失败**。
> 这样「清单过期了」不会悄悄发生。
>
> 问题 1 / 2 / 3 不在这张表里——它们不是 `rule_config` 参数，
> 分别落在 `metric_definition.sign_convention`、`engine/narrative.py::_THEMES`、
> 解析层的符号处理里。

---

## ⚠ 为什么这里用的是「问题 1–7」，不是「A-1 …」

**因为 `A-数字` 已经被会计文档占用了。** `accounting_signoff_v1.docx` 里的编号是：

```
A-1 字段键名    A-2 revenue 口径     A-3 应收票据及应收账款
A-4 吨钢毛利    A-5 产能利用率       A-6 周期正常化阈值
A-7 诊断指数    A-8 rule_config 处理方式    A-9 细分行业
```

这份文件一度给自己的条目也编了 `A-1 … A-6`，**和上面撞了四个**——
会计同学读到「A-5」会想到产能利用率，而这里指的是计分权重。
**同一批编号两个含义，而且不会报错。** 已全部改掉。

引用 K 的文档时继续用 `A-数字`（如「依据 A-7」），引用本文条目时一律用「问题 N」。

---

## ⚠ 自己拍过的那个先例

`engine/narrative.py` 里曾有一道 1% 的幅度阈值（`narrative.min_rel_change`），
就是「为了让判断结果不那么难看」自己加的，结果和 A-7 直接冲突，
已于 2026-09-26 删除，全样本冲突数 8 → 10。

**需要某个值定下来才能往下做 → 去问，不要自己拍。**
噪声该在「这句话够不够格被当成主张」那一层挡（词表与排除词），不在判定那一层。
