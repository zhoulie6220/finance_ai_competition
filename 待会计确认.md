# 待会计确认

> **这份文件是「还没经过会计签字的东西」的唯一真源。**
> 签名栏见 `accounting_signoff_v1.docx` §8（三个位置全空）。
>
> 建立于 2026-09-26。在此之前，下面这些东西散在 `rule_config` 的种子文件里、
> `engine/narrative.py` 的主题表里、`parsing/` 的正负号处理里——
> **每一条都标着 `tier='hard'`（会计口径须签字），但没有任何一份文档给过依据。**

---

## 怎么用这份清单

**给计算机方向的同学（和你的 AI）**

- `rule_config` 里 `description` 带 **`⬜ 待会计确认`** 前缀的参数，就是这里列的。
  **不要自己给它们定值，也不要照着某个值往下实现就当成最终口径。**
- 你可以在代码里用它（它们当前是生效的），但**结论里要能看出它是暂定值**——
  界面上尤其：拿暂定权重算出来的估值，和一个没人确认过的数字，评审分不出来。
- 需要这些值定下来才能往下做的，**去问 K**，不要自己拍。

**给会计方向的同学**

- 逐条给个结论就行，不用改代码，不用看代码。
- 每条都写了「定完改哪里」，你答完由计算机同学落到那个位置。
- 不确定的可以答「暂按现值，决赛前再定」——**但要说出来**，
  不说的话它和「已经签过字」在库里长得一模一样。

**为什么这份清单必须存在**

`rule_config.tier` 只有三档（A-8：`hard` 硬规则 / `soft` 软规则 / `model` 模型参数），
**没有「已经实现但没签字」这一档**。于是那些实现时先拍的数字，
因为 `tier` 默认 `hard`，看起来和其他会计师口径的参数**完全一样**。
机器分不出来，只能靠这份人工核过的表。

---

## 被标记的参数

`rule_config` 里 `description` 带 `⬜ 待会计确认` 前缀的，就是下面这些：

| 参数 | 现值 | 对应条目 |
|---|---|---|
| `index.direction.weak_ratio` | `0.5` | A-5 |
| `index.weight.supported` | `1.0` | A-5 |
| `index.weight.partial` | `0.5` | A-5 |
| `index.weight.conflicted` | `-1.0` | A-5 |
| `mapping.high.weights` | `[0.60,0.25,0.15]` | A-4 |
| `mapping.high.delta` | `historical_median` | A-4 |
| `mapping.medium.weights` | `[0.50,0.20,0.30]` | A-4 |
| `mapping.medium.delta` | `historical_p25` | A-4 |
| `mapping.low.weights` | `[0.35,0.15,0.50]` | A-4 |
| `mapping.low.delta` | `historical_p25_x0.9` | A-4 |
| `valuation.terminal_growth_max` | `0.02` | A-6 |
| `valuation.wacc_min` | `0.06` | A-6 |

> 这张表和种子文件由 `backend/tests/unit/db/test_pending_accounting.py` 对账：
> 种子里有标记而表里没有、或表里有而种子没标记，**都会测试失败**。
> 这样「清单过期了」不会悄悄发生。
>
> A-1 / A-2 / A-3 不在这张表里——它们不是 `rule_config` 参数，
> 分别落在 `metric_definition.sign_convention`、`engine/narrative.py::_THEMES`、
> 解析层的符号处理里。

---

## 一、必须拍板（会直接改变系统算出来的结果）

### A-1 正负号口径 🔴

**现状**：同一个科目在不同年份的年报里列报方向相反。

| 科目 | 2015 年报 | 2024 年报 |
|---|---|---|
| `impairment_loss`（资产减值损失） | 印**正数**（损失额） | 印**负数**（抵减利润） |

**已经有的**：`metric_definition.sign_convention`，取值域
`positive_is_good` / `negative_is_good` / `neutral`，CHECK 约束已建。
`schema.sql:132`

**缺什么**：**哪些科目该标哪个值，一份文档都没写过。**
已逐份核对 `accounting_signoff_v1.docx` / `方案选择.docx` /
`大框架-投研程序设计初版.docx` / `信息-搭建具体步骤.docx` / `exclusion_terms_and_sentence.docx`——
「正负号」「符号」「sign_convention」在这些文件里**一次都没出现**。

**不定会怎样**：跨年比大小会把 `+3亿` 和 `−3亿` 当成方向相反。
资产减值损失在钢铁行业是常态科目，**十年里必然跨过口径切换**，
所以这不是边角情况。而且它不会报错——只会让某个主题的「冲突」凭空多出来。

**定完改哪里**：`app/data/seed/0001_metric_definitions.sql` 的
`sign_convention` 列；解析层按它归一（`parsing/`）。

---

### A-2 五个叙事主题各用哪个指标验证 🔴

**现状**：主题词本身有依据（`大框架` 里有「需求旺盛」「降本增效」「产品结构」
「回款」「产能释放」），**但「哪个主题拿哪个指标去验」是实现时定的**。

| 主题 | 当前验的指标 | 文档依据 |
|---|---|---|
| 需求与产销 | 营业收入 | ❌ 无 |
| 产能与效率 | 营业收入 | ❌ 无 |
| 降本增效 | 毛利率 | ❌ 无 |
| 产品结构升级 | 毛利率 | ❌ 无 |
| 回款与现金流 | 经营活动现金流量净额 | ❌ 无 |

**位置**：`app/engine/narrative.py::_THEMES` 的 `metric` 字段

**不定会怎样**：**指标选错，「叙事与事实相悖」就是误报。**
这是整个项目的核心结论，评委一定会问「为什么降本增效验的是毛利率而不是成本额」。
另外「毛利率」是派生量（毛利 ÷ 营业收入），需要两个原始科目，
所以选它意味着证据面板里要同时给出被减数和减数——这一层是为了可核对性。

**定完改哪里**：`_THEMES` 的 `metric` 与 `basis_cn`（`basis_cn` 会显示在证据面板里）。

---

### A-3 「产能释放」算不算利好 🟡

**现状**：**刻意没有收进词表。**

**为什么不收**：宝钢 2024 年报原话——
「市场有效需求不足、钢铁产能释放较快，铁矿石价格高位运行，行业盈利空间受到挤压」。
**产能释放在这里是利空**（说的是供给过剩）。按词面当利好主张，
系统会在一份基调悲观的年报上判出「管理层看好产能扩张」。

**不定会怎样**：不收 = 这一主题的主张一律识别不出来。目前判断是「宁缺勿滥」，
但**这是会计/行业判断，不是工程判断**。

**定完改哪里**：`_THEMES` 里 `capacity` 主题的 `keywords` / `exclude`
（以及同类的「需求」——「需求不足」和「需求旺盛」共用两个字）。

---

### A-4 情景传导的整套数字 🔴

**现状**：指数分级 → 估值情景权重与参数调整，**每一个数字都没有依据**。

| 分级 | 情景权重（基准/乐观/压力） | 参数调整 |
|---|---|---|
| 高 | `[0.60, 0.25, 0.15]` | 增速取历史中位数，WACC 与终值增速不变 |
| 中 | `[0.50, 0.20, 0.30]` | 增速取历史下四分位，WACC +0.5 个百分点 |
| 低 | `[0.35, 0.15, 0.50]` | 增速取下四分位再打九折，WACC +1.0 个百分点，终值增速 −0.5 个百分点 |
| 不足 | 不传导 | 不自动改变任何参数，只增加人工复核提示 |

**依据情况**：五份 Word 对传导**只描述了方向**——
`大框架` 原文是「保持基准情景；**可提高**基准情景权重」
「降低收入/利润增长假设，**或**提高压力情景权重」。
**一个具体数字都没有。** 这些是实现时先拍的。

**不定会怎样**：它们**直接决定估值区间**。周期股估值的核心争议就在情景权重，
而这里的数字会让「叙事一致性低」自动把压力情景权重推到 50%——
这个判断的量级没有会计依据。

**定完改哪里**：`app/data/seed/0002_rule_config.sql` 的 `mapping.*` 四组参数；
`docs/04-index-rules.md` §五的表格；`engine/index.py::map_index_to_scenario()`
（未实现，乙负责）。

---

### A-5 观测状态的计分权重 🟡

**现状**：

```
index.weight.supported    1.0
index.weight.partial      0.5
index.weight.conflicted  -1.0
index.direction.weak_ratio 0.5   ← 「幅度较弱」的分界线
```

**依据情况**：`方案选择.docx` §10 只说了
「方向一致但幅度较弱，标记为部分支持」，**没给比例**。
`weak_ratio = 0.5` 是实现时拍的。

**不定会怎样**：这三个权重直接决定 `H`（历史兑现度）与 `C`（当前一致性）的数值。
**冲突的负权重定多重，等于定了「一次冲突扣多少分」。**

另注：`partial` 这一态**引擎目前产不出来**——判「幅度较弱」要拿实际幅度
比一个目标幅度，而当前只抽取方向性主张，没有数值目标
（`weak_ratio` 的描述里写的「无目标时取历史同类主张中位幅度」需要一个主张库）。
等 LLM 抽取版一起做。**不要为了让这个状态有输出而在代码里凑一个阈值。**

**定完改哪里**：`0002_rule_config.sql` 的 `index.weight.*` 与 `index.direction.weak_ratio`。

---

### A-6 估值的两条硬约束 🟡

| 参数 | 现值 | 含义 |
|---|---|---|
| `valuation.terminal_growth_max` | `0.02` | 周期行业不得给高永续增速，上限取长期通胀 |
| `valuation.wacc_min` | `0.06` | 防止压力情景下 WACC 被推到不切实际的水平 |

**依据情况**：方向对（周期股估值硬规则），但 `0.02` / `0.06` 这两个具体数字是拍的。

**定完改哪里**：`0002_rule_config.sql`。

---

## 二、需要确认后果

### B-1 删掉幅度阈值后，宝钢 2020 那两条被判成冲突

**背景**：`engine/narrative.py` 里曾经有一道 1% 的幅度闸门
（`narrative.min_rel_change`），实现时拍的，**没有会计依据**。
它与 A-7 直接冲突——A-7 原文：

> 「20 个百分点」**只适用于** MD&A 明确提出数值目标的情况。对于「需求增长」
> 「回款改善」等方向性表述，实际方向相反，**直接标记为冲突**。

已于 2026-09-26 按 A-7 删除。

**删除的后果**：下面这两条从「不可比」变成「冲突」，全项目冲突数 **8 → 10**。

```
[降本增效] 2020 年｜毛利率 10.8791% → 10.8350%（−0.04 个百分点）
    原句：2020年，公司抓住疫情后钢铁产能恢复契机，持续提升生产效率、资金效率
          与资产效率，深挖成本削减潜力，有效缓解了钢…
```

**需要确认的**：按 A-7 字面执行就是这个结果。**如果认为 0.04 个百分点这个量级
不该算冲突，需要明确写进文档**（例如「方向性主张的幅度下限为 X 个百分点」）。

> ⚠ **不要再在代码里悄悄加回阈值。** 上一版那个 1% 就是这么来的——
> 挡噪声的动机是对的，但那是拿工程直觉改会计口径，而且它会让一条真实的反向主张
> 因为「动得不够多」而免于被记成冲突，**在界面上与「数据缺失」长得一模一样**。

---

## 三、不用会计同学管的（免得白花时间）

| 事项 | 结论 |
|---|---|
| `docs/04-index-rules.md` | 内容全在 A-7 里，2026-09-26 已转写。**不是欠交的东西** |
| 参数条数 65 vs 66 | 对得上。中间改过一次（删了 2 条恒为真的分位数参数、加了 1 条 `phase_min_spread`），加上实现时多加了 2 条叙事参数；按 A-7 删掉 1 条后现在正好 65 |
| `narrative.min_rel_change` | 无依据的自造参数，已删除。见 B-1 |

---

## 四、确认完之后

1. 改上表「定完改哪里」指的位置
2. 按 `CLAUDE.md` 的三步重跑：
   `init_db.py --force` → `parse_reports.py --source var/samples` → `parse_mdna.py`
3. 把 `rule_config` 里那 12 条参数的 `description` 前缀 **`⬜ 待会计确认`** 去掉
   （去掉 = 声明它已过会计确认，**不是顺手清理**）
4. 删掉本文对应的小节
5. 在 `accounting_signoff_v1.docx`（或它的下一版）签字

> 第 3 步由 `backend/tests/unit/db/test_pending_accounting.py` 盯着：
> 带 `⬜` 标记的参数必须在本文里出现，两边分叉会直接测试失败。
