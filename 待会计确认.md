# 需要会计同学确认的 7 件事

> 只用回答，**不用改代码、不用打开仓库、不用查资料**。
> 每件事下面写一句结论就行。

---

## 怎么答

- **一句话即可。** 格式随意，编号对上就行。
- 不确定的可以写「**暂按现值，决赛前再定**」——但请写出来。
  不写的话，它和「已经确认过」在系统里长得一模一样，没人分得出来。
- 下面表格里的数字是**系统现在实际在用的**，不是我们建议的值。

## 这些问题为什么得由你定

系统会算出一个「叙事一致性」分数，再拿它去调整估值。但有 **12 个参数**是写代码的
同学为了先跑起来自己填的，**没有任何会计依据**。

它们在系统里和「已经确认过」的参数**完全一样**，机器分不出来——所以只能靠你来核。
其中几个直接决定估值区间（也就是说，会出现在给评审看的结论里）。

---

## 问题 1｜同一个科目，年报里正负号不一致，按哪种理解？

同一项「资产减值损失」，不同年份的年报印的方向是反的：

| | 2015 年报 | 2024 年报 |
|---|---|---|
| 资产减值损失 | 印**正数**（损失了多少） | 印**负数**（抵减了多少利润） |

这是年报列报习惯的变化，不是数据错误。但系统要把十年的数放在一起比大小，
而 `+3 亿` 和 `−3 亿` 在比大小时方向完全相反。

**要你定的**：这类科目（不只是资产减值损失）该按哪种口径统一？
哪些科目算「正数表示好」、哪些算「负数表示好」？

答：

---

## 问题 2｜管理层说了某类话，该拿哪张表去验？

系统的做法是：从年报「管理层讨论与分析」里认出管理层的说法，再拿一个财务指标去对。
**认哪些说法**这件事有依据（你们给的设计文档里列了五类），
但**每类说法该拿哪个指标去验，是写代码的同学自己配的**：

| 管理层说的 | 现在拿这个指标验 |
|---|---|
| 需求旺盛、产销两旺 | 营业收入 |
| 满负荷生产、产能利用率提升 | 营业收入 |
| 降本增效、成本削减 | 毛利率 |
| 产品结构优化、高端产品 | 毛利率 |
| 回款改善、现金流改善 | 经营活动现金流量净额 |

**要你定的**：这五组配对对不对？**配错了，「叙事与事实相悖」就是误报**——
而这正是整个系统要给评审看的核心结论。

特别想问的一个：**「降本增效」为什么验毛利率而不是成本额本身？**
现在的理由是「成本额会随产量一起涨，涨得比收入慢才叫降本」。
评委很可能会问这一条。

答：

---

## 问题 3｜「产能释放」算利好还是利空？

系统的词表里**没有收**「产能释放」。原因是宝钢 2024 年报的原话是：

> 市场有效需求不足、**钢铁产能释放较快**，铁矿石价格高位运行，行业盈利空间受到挤压

在这个语境里产能释放说的是供给过剩，**是利空**。如果按字面当成「公司看好产能扩张」，
系统会在一份基调悲观的年报上判出乐观结论。

**要你定的**：不收，对吗？（不收的代价是这一类说法一条都识别不出来。）

答：

---

## 问题 4｜分数低的时候，估值该往悲观调多少？

系统算出分数后，按下表调整估值（三档情景的权重，以及增长率、折现率的变化）：

| 分数 | 情景权重（基准/乐观/压力） | 参数怎么调 |
|---|---|---|
| 高（≥70） | 60% / 25% / 15% | 增长率取历史中位数，折现率与永续增长率不动 |
| 中（45–69） | 50% / 20% / 30% | 增长率取历史下四分位，折现率 +0.5 个百分点 |
| 低（<45） | 35% / 15% / 50% | 增长率取下四分位再打九折，折现率 +1.0 个百分点，永续增长率 −0.5 个百分点 |
| 证据不足 | 不调 | 什么参数都不动，只提示人工复核 |

**要你定的**：整张表的数字。你们给的文档里对这部分**只写了方向**
（原话是「保持基准情景；**可提高**基准情景权重」），**一个具体数字都没有**，
所以这些是写代码的同学先填的。

最需要你表态的是：**分数一低就把压力情景的权重推到 50%**，这个量级合适吗？

答：

---

## 问题 5｜一次「叙事与事实相悖」，该扣多少分？

分数由三类观测构成，现在按这个权重加总：

| 观测 | 权重 |
|---|---|
| 被财务事实验证 | `1.0` |
| 方向一致但幅度偏弱（部分支持） | `0.5` |
| 与财务事实相悖 | `−1.0` |

另外「幅度偏弱」的分界线现在定在 `0.5`（实际幅度不到目标幅度的一半就算部分支持）。

**要你定的**：这三个权重。**「相悖」的负权重定多重，等于定了「一次冲突扣多少分」。**
你们给的文档只说了「方向一致但幅度较弱，标记为部分支持」，没给具体比例。

答：

---

## 问题 6｜估值的两个上下限

| 参数 | 现值 | 是什么意思 |
|---|---|---|
| 永续增长率上限 | `2%` | 周期行业不给高永续增速，按长期通胀封顶 |
| 折现率下限 | `6%` | 防止压力情景下折现率被推到不切实际的水平 |

方向是你们定的（周期股估值硬规则），但 `2%` 和 `6%` 这两个数字是写代码的同学填的。

答：

---

## 问题 7｜有一个后果，需要你确认能不能接受

写代码的同学原先在系统里放了一道「小幅波动不算数」的规则——**变动不到 1% 就当作没动**。
理由是这样两条会被判成冲突：

```
[降本增效] 2020 年｜毛利率 10.8791% → 10.8350%（-0.04 个百分点）
```

但这条规则和你们给的文档**直接冲突**。文档原文是：

> 「20 个百分点」只适用于明确提出数值目标的情况。对于「需求增长」「回款改善」
> 等方向性表述，**实际方向相反，直接标记为冲突**。

所以那道 1% 的规则已经**删掉了**。删掉之后，上面那条真的被判成了冲突，
全样本的冲突数从 8 条变成 10 条。

**要你确认的**：0.04 个百分点这个量级，判成「叙事与事实相悖」能接受吗？

- 能接受 → 不用做什么，现在的行为就是对的
- 不能接受 → **请在文档里写一条明确的下限**（例如「方向性主张的幅度下限为 X 个百分点」）

答：

---

## 附：不用管的（免得白花时间）

| 事情 | 情况 |
|---|---|
| `docs/04-index-rules.md` 一直没交 | 是个误会。指数公式你们 9-22 就给了，只是没人把它转成文档，现在已经转好 |
| 参数数量对不上（65 还是 66） | 对得上，中间改过一次，现在是 65，和文档里说的一致 |
| 系统里有个「1% 阈值」 | 已按你们的文档删除，见问题 7 |

---
---

# （以下给计算机同学，会计同学可以跳过）

## 被标记的参数

`rule_config` 里 `description` 带 `⬜ 待会计确认` 前缀的，就是上面这些问题涉及的参数：

| 参数 | 现值 | 对应问题 |
|---|---|---|
| `index.direction.weak_ratio` | `0.5` | 问题 5 |
| `index.weight.supported` | `1.0` | 问题 5 |
| `index.weight.partial` | `0.5` | 问题 5 |
| `index.weight.conflicted` | `-1.0` | 问题 5 |
| `mapping.high.weights` | `[0.60,0.25,0.15]` | 问题 4 |
| `mapping.high.delta` | `historical_median` | 问题 4 |
| `mapping.medium.weights` | `[0.50,0.20,0.30]` | 问题 4 |
| `mapping.medium.delta` | `historical_p25` | 问题 4 |
| `mapping.low.weights` | `[0.35,0.15,0.50]` | 问题 4 |
| `mapping.low.delta` | `historical_p25_x0.9` | 问题 4 |
| `valuation.terminal_growth_max` | `0.02` | 问题 6 |
| `valuation.wacc_min` | `0.06` | 问题 6 |

> 这张表和种子文件由 `backend/tests/unit/db/test_pending_accounting.py` 双向对账：
> 种子里有标记而表里没有、或表里有而种子没标记，**都会测试失败**。
> 这样「清单过期了」不会悄悄发生。
>
> 问题 1 / 2 / 3 不在这张表里——它们不是 `rule_config` 参数，
> 分别落在 `metric_definition.sign_convention`、`engine/narrative.py::_THEMES`、
> 解析层的符号处理里。

---

## ⚠ 为什么这里用的是「问题 1–7」，不是「A-1 …」

**因为 `A-数字` 已经被会计文档占用了。** `accounting_signoff_v1.docx` 里的编号是：

```
A-1 字段键名    A-2 revenue 口径     A-3 应收票据及应收账款
A-4 吨钢毛利    A-5 产能利用率       A-6 周期正常化阈值
A-7 诊断指数    A-8 rule_config 处理方式    A-9 细分行业
```

这份文件一度给自己的条目也编了 `A-1 … A-6`，**和上面撞了四个**——
会计同学读到「A-5」会想到产能利用率，而这里指的是计分权重。
**同一批编号两个含义，而且不会报错。** 已全部改掉。

引用 K 的文档时继续用 `A-数字`（如「依据 A-7」），引用本文条目时一律用「问题 N」。

---

## 各问题的落地位置

| 问题 | 落地位置 |
|---|---|
| 1 | `0001_metric_definitions.sql` 的 `sign_convention`；解析层按它归一 |
| 2 | `engine/narrative.py::_THEMES` 的 `metric` 与 `basis_cn` |
| 3 | `_THEMES` 里 `capacity` 主题的 `keywords` / `exclude` |
| 4 | `0002_rule_config.sql` 的 `mapping.*`；`docs/04-index-rules.md` §五；`engine/index.py::map_index_to_scenario()`（未实现，乙负责） |
| 5 | `0002_rule_config.sql` 的 `index.weight.*` 与 `index.direction.weak_ratio` |
| 6 | `0002_rule_config.sql` 的 `valuation.terminal_growth_max` / `wacc_min` |
| 7 | 已执行（删除了 `narrative.min_rel_change`）；只需确认，或补一条明确下限 |

## 收到答复之后

1. 改上表「落地位置」指的地方
2. 按 `CLAUDE.md` 的三步重跑：
   `init_db.py --force` → `parse_reports.py --source var/samples` → `parse_mdna.py`
3. 把 `rule_config` 里那 12 条参数的 `description` 前缀 **`⬜ 待会计确认`** 去掉
   （去掉 = 声明它已过会计确认，**不是顺手清理**）
4. 删掉本文对应的小节
5. 在 `accounting_signoff_v1.docx`（或它的下一版）签字

> ⚠ **问题 2 / 3 这一层不要自己拍。** `engine/narrative.py` 里曾有一道 1% 的幅度阈值
> （`narrative.min_rel_change`），就是「为了让判断不那么难看」自己加的，
> 结果和 A-7 直接冲突，已于 2026-09-26 删除。
> 需要这些值定下来才能往下做 → **去问，不要自己拍**。
